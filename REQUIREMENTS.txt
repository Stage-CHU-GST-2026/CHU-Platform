# CHU-Platform — Requirements
# ============================
# Projet : Data Analyst Agent Platform
# Architecture : Backend (FastAPI) + Frontend (SvelteKit) + Agents IA (LangGraph)
# Python >= 3.10 requis

# ─────────────────────────────────────────────
# 1. Dépendances principales (pyproject.toml root)
# ─────────────────────────────────────────────
langchain>=0.3.0
langchain-openai>=1.3.5
langgraph>=1.2.9
python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
pydantic>=2.13.4
matplotlib>=3.7.0
pyyaml>=6.0
structlog>=26.1.0
tenacity>=9.1.4

# ─────────────────────────────────────────────
# 2. Dépendances API (apps/api/pyproject.toml)
# ─────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sse-starlette>=2.0.0
pydantic-settings>=2.14.0

# ─────────────────────────────────────────────
# 3. Dépendances de développement
# ─────────────────────────────────────────────
pytest>=7.0
pytest-cov>=4.0
ruff>=0.1.0
mypy>=1.0
pre-commit>=3.0

# ─────────────────────────────────────────────
# 4. Dépendances Frontend (apps/web/package.json)
# ─────────────────────────────────────────────
# Runtime :
#   @fontsource-variable/inter
#   @lucide/svelte
#   @shikijs/themes
#   ai (Vercel AI SDK)
#   mode-watcher
#   runed
#   streamdown-svelte
#
# Dev :
#   svelte ^5.56.1
#   @sveltejs/kit ^2.63.0
#   @sveltejs/adapter-node
#   tailwindcss ^4.3.0
#   shadcn-svelte ^1.4.1
#   bits-ui ^2.16.3
#   typescript ^6.0.3
#   vite ^8.0.16
#   vitest ^4.1.8
#   prettier, eslint, playwright
#   phosphor-svelte
#   clsx, tailwind-merge, tailwind-variants
#   tw-animate-css
#   mdsvex
#   @internationalized/date
#   @tailwindcss/forms, @tailwindcss/typography, @tailwindcss/vite

# ─────────────────────────────────────────────
# 5. Installation
# ─────────────────────────────────────────────
# Backend (Python) :
#   uv sync                     # installe toutes les dépendances Python
#   uv run example.py           # lance la démo CLI
#   uv run --directory apps/api python apps/api/run.py   # lance l'API
#
# Frontend (Svelte) :
#   cd apps/web
#   npm install
#   npm run dev

# ─────────────────────────────────────────────
# 6. Variables d'environnement (.env à la racine)
# ─────────────────────────────────────────────
# OPENAI_BASE_URL=http://localhost:6060/v1
# OPENAI_API_KEY=your-api-key-here
